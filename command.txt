# Commands in Dev-env:

docker build -t video_trans .

docker images

# commands inside Docker env:

docker run -it -v /Users/sahilwep/Developer/Development/Github_Space/HLS-ABR-Implementation/videos:/home/app/vidoes 7c1e0ec2a7b6




# ffmpeg Commands sample:

ffmpeg -i input.mp4  -codec:v libx264 -codec:a aac -hls_time 10 -hls_playlist_type vod -hls_segment_filename "${outputPath}/segment%03d.ts" -start_number 0 ${hlsPath}

once the segments are created, we can't open it, because this is binary files, not typescript files..

create another video sample...

ffmpeg -i sample.mov  -codec:v libx264 -codec:a aac -hls_time 10 -hls_playlist_type vod -hls_segment_filename outputs_2/segment%03d.ts -start_number 0 outputs_2/index.m3u8




# now once the segments are created, we can upload in s3 or we can serve from local storage..

# we can visit videojs.com (open-source tool that support hls)
https://videojs.com/getting-started

# resource...
https://videojs.github.io/videojs-contrib-hls/


